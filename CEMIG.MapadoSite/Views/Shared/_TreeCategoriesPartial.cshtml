@model CEMIG.MapadoSite.Models.SeededMenusModel

<div>
    <ul class="nav nav-list">
        <li class="tree-toggle nav-header">
            @if (Model.Menus.Where(s => s.Parent_ID == Model.Seed).Any())
            {
            <label class="tree-toggle nav-header"><i class="fa fa-arrow-right"></i></label>
                <ul class="nav nav-list tree">
                    @foreach (var node in Model.Menus)
                    {
                        if (node.Parent_ID == Model.Seed)
                        {
                            CEMIG.MapadoSite.Models.SeededMenusModel inner = new CEMIG.MapadoSite.Models.SeededMenusModel { Seed = node.ID, Menus = Model.Menus };

                            @if (!string.IsNullOrEmpty(node.Link))
                            {
                                <li class="nav tree">
                                    <a target="_blank" href="@(node.Link == null ? "#" : node.Link)">@node.Name</a>&nbsp;
                                    <div class="col-6 col-sm-4 col-md mb-3 mb-xl-0 text-center">
                                        <a class="btn btn-light" href="@Url.Action("MenuDetail", "Home")?id=@node.ID" target="_blank">
                                            <i class="fa cui-pencil icons" style="background-color: @node.Color"></i>&nbsp;Avaliar Página
                                        </a>
                                    </div>
                                </li>
                            }
                            else
                            {    
                                <li class="tree-toggle nav-header">
                                    <a>@node.Name</a>
                                </li>
                            }

                            @Html.Partial("_TreeCategoriesPartial", inner)
                        }
                    }
                </ul>
            }
        </li>
    </ul>
</div>


